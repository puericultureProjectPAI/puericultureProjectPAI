server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: projet-puericulture

  # Configuration de l'IA (OpenRouter / Ollama)
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}

  # Configuration PostgreSQL (Supabase)
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      # CRITIQUE EN LOCAL : Autorise Hibernate à créer/mettre à jour tes tables métier
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true # Pratique pour le debug en local
  # Limite de taille d'image pour ne pas tuer les instances gratuites de Render et supabase
  servlet:
    multipart:
      max-file-size: ${SUPABASE_STORAGE_MAX_SIZE:10MB}
      max-request-size: ${SUPABASE_STORAGE_MAX_SIZE:10MB}

# Configuration Supabase (Correspond EXACTEMENT aux @Value de ton JwtService)
supabase:
  jwt:
    secret: ${SUPABASE_JWT_SECRET}
  url: ${SUPABASE_URL}
  service:
    key: ${SUPABASE_SERVICE_KEY}
  storage:
    bucket: ${SUPABASE_STORAGE_BUCKET:puericulture-images}

# Documentation API (Swagger)
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

# Monitoring
management:
  endpoints:
    web:
      exposure:
        include: health,info

# Gestion des logs
logging:
  level:
    root: INFO
    org.springframework.security: INFO
    # Correction du nom de package pour correspondre à tes classes Java
    com.puericulture: DEBUG